# 鸿蒙视频横竖屏切换工程结构说明
## 一、工程概述
本工程是基于HarmonyOS NEXT的视频播放示例应用，实现了**页面内横竖屏切换**和**页面级横竖屏切换**两种主流视频播放场景，采用ArkTS语言和ArkUI声明式开发范式，基于Stage模型开发。

## 二、整体目录结构
VideoLandscapePortraitSwitching/
├── AppScope/ # 应用全局配置目录
│ ├── app.json5 # 应用全局配置（包名、版本、图标等）
│ └── resources/ # 应用全局资源（图标、字符串等）
├── entry/ # 主模块目录
│ ├── src/main/
│ │ ├── ets/ # ArkTS 源代码目录
│ │ │ ├── common/ # 通用工具类目录
│ │ │ ├── entryability/ # 应用入口 Ability
│ │ │ ├── entrybackupability/ # 数据备份恢复类
│ │ │ ├── model/ # 数据模型目录
│ │ │ ├── pages/ # 应用首页
│ │ │ ├── transitionbetweenpage/ # 页面级横竖屏切换目录
│ │ │ ├── transitioninpage/ # 页面内横竖屏切换目录
│ │ │ ├── utils/ # 核心工具类目录
│ │ │ └── views/ # 通用 UI 组件目录
│ │ ├── resources/ # 模块资源目录（布局、图片等）
│ │ └── module.json5 # 模块配置文件
│ └── oh-package.json5 # 模块依赖配置
├── hvigor/ # Hvigor 构建工具目录
├── hvigorfile.ts # 构建脚本
├── oh-package.json5 # 工程全局依赖配置
└── video1_basic_function.mp4 # 基础功能运行录屏

## 三、核心文件详细说明
### 1. 配置文件
- **app.json5**：定义应用包名`com.huawei.videolandscapeportraitswitching`、版本号1.0.0、应用图标和名称等全局信息
- **module.json5**：声明应用入口Ability、所需权限（本应用无需特殊权限）、设备类型支持等
- **hvigorfile.ts**：Hvigor构建工具配置文件，引入鸿蒙官方构建插件

### 2. 核心源代码文件
| 文件路径 | 功能说明 |
|---------|---------|
| `common/Logger.ets` | 统一日志打印工具类，封装了不同级别日志的输出方法 |
| `common/CustomTransition.ets` | 自定义转场动画管理类，实现页面级切换的"一镜到底"效果 |
| `common/VideoNodeController.ets` | 视频组件自定义节点管理类，负责视频视图的创建和销毁 |
| `entryability/EntryAbility.ets` | 应用入口Ability，处理应用的启动、前后台切换等生命周期事件 |
| `pages/Index.ets` | 应用首页，提供两个场景的入口按钮 |
| `transitioninpage/VideoDetail.ets` | 页面内横竖屏切换的视频详情页 |
| `transitioninpage/VideoPlayView.ets` | 页面内视频播放核心组件，实现窗口方向切换逻辑 |
| `transitionbetweenpage/DetailPlay.ets` | 页面级横竖屏切换的视频详情页 |
| `transitionbetweenpage/LandscapePlay.ets` | 页面级横屏全屏播放页面 |
| `utils/AVPlayerUtil.ets` | 视频播放核心工具类，封装AVPlayer的初始化、播放、暂停、销毁等操作 |
| `utils/WindowUtils.ets` | 窗口工具类，封装窗口方向设置、窗口属性获取等方法 |
| `views/BottomView.ets` | 视频播放底部操作栏组件，包含播放/暂停、进度条、全屏按钮等 |

## 四、核心技术实现原理
1. **视频播放**：基于鸿蒙系统AVPlayer组件实现，封装在AVPlayerUtil工具类中
2. **页面内横竖屏切换**：调用Window对象的`setPreferredOrientation`方法动态改变窗口方向
3. **页面级横竖屏切换**：使用Navigation组件的`customNavContentTransition`自定义转场动画能力
4. **一镜到底动画**：通过属性动画实现视频视图从详情页位置旋转放大到全屏的过渡效果

## 五、运行环境要求
- HarmonyOS系统版本：6.0.2 Release及以上
- DevEco Studio版本：6.0.2 Release及以上
- 支持设备：华为标准系统手机